% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sdmTMB_engine.R
\name{sdmTMB_gp_predict}
\alias{sdmTMB_gp_predict}
\title{Predict from an sdmTMB-fitted Gaussian Process model}
\usage{
sdmTMB_gp_predict(
  object,
  new_data,
  type = "numeric",
  level = 0.95,
  coord_cols = NULL,
  ...
)
}
\arguments{
\item{object}{A \code{"gopher_sdmTMB_fit"} object returned by
\code{\link[=sdmTMB_gp_fit]{sdmTMB_gp_fit()}}.}

\item{new_data}{An \code{sf} object or \code{data.frame} with the same coordinate
structure (and covariate columns, if any) as the training data.}

\item{type}{One of \code{"numeric"} (default, returns \code{.pred}) or
\code{"pred_int"} (returns \code{.pred}, \code{.pred_lower}, \code{.pred_upper}).}

\item{level}{Confidence level for prediction intervals (default \code{0.95}).}

\item{coord_cols}{Character vector of length 2 giving coordinate column
names for non-\code{sf} new data. \code{NULL} uses the coord_cols from the fit.}

\item{...}{Additional arguments forwarded to \code{predict.sdmTMB()}.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with prediction columns.
}
\description{
Predict from an sdmTMB-fitted Gaussian Process model
}
\examples{
if (requireNamespace("sdmTMB", quietly = TRUE)) {
  set.seed(1)
  train <- data.frame(x = runif(50, 0, 10), y = runif(50, 0, 10))
  train$z <- sin(train$x) + cos(train$y) + rnorm(50, 0, 0.3)
  train_sf <- sf::st_as_sf(train, coords = c("x", "y"), crs = 4326)

  newdat <- sf::st_as_sf(
    expand.grid(x = seq(1, 9, l = 4), y = seq(1, 9, l = 4)),
    coords = c("x", "y"), crs = 4326
  )

  fit   <- sdmTMB_gp_fit(z ~ 1, data = train_sf)
  sdmTMB_gp_predict(fit, new_data = newdat)
}

}
