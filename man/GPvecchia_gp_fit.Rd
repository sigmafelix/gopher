% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/GPvecchia_engine.R
\name{GPvecchia_gp_fit}
\alias{GPvecchia_gp_fit}
\title{Fit a Gaussian Process model using the GPvecchia engine}
\usage{
GPvecchia_gp_fit(
  formula,
  data,
  covariance_function = "exponential",
  range = NULL,
  nugget = NULL,
  sill = NULL,
  m = 15L,
  coord_cols = NULL,
  time_col = NULL,
  time_scale = 1,
  ...
)
}
\arguments{
\item{formula}{A two-sided formula. Covariates are used as fixed-effect
trend (Universal Kriging / residual GP).}

\item{data}{An \code{sf} object or \code{data.frame} with coordinate columns.}

\item{covariance_function}{Canonical covariance name. Defaults to
\code{"exponential"}.}

\item{range}{Range parameter (scale). \code{NULL} = estimated via MLE.}

\item{nugget}{Nugget (noise) variance. \code{NULL} = estimated.}

\item{sill}{Signal variance (sigma^2). \code{NULL} = estimated.}

\item{m}{Number of nearest neighbours for Vecchia approximation.
Default \code{15}.}

\item{coord_cols}{Character(2) coordinate column names (non-sf path).}

\item{time_col}{Optional character scalar specifying a time column for
spatiotemporal modelling.}

\item{time_scale}{Numeric scalar used to rescale time when \code{time_col} is
provided. Default \code{1}.}

\item{...}{Additional arguments forwarded to
\code{GPvecchia::vecchia_specify()}.}
}
\value{
A list of class \code{"gopher_GPvecchia_fit"}.
}
\description{
This function is called internally by parsnip. End users should call
\code{\link[parsnip:fit]{parsnip::fit.model_spec()}} on a \code{gaussian_process_spatial} specification
with \code{set_engine("GPvecchia")}.
}
\examples{
if (requireNamespace("spacetime", quietly = TRUE) &&
    requireNamespace("GPvecchia", quietly = TRUE)) {
  data("air", package = "spacetime")

  day_id <- which.max(colSums(!is.na(air)))
  air_day <- data.frame(
    station = rownames(air),
    pm10 = air[, day_id],
    day = dates[day_id],
    sp::coordinates(stations)
  )
  air_day <- air_day[stats::complete.cases(air_day$pm10), ]
  air_sf <- sf::st_as_sf(
    air_day,
    coords = c("coords.x1", "coords.x2"),
    crs = 4326,
    remove = FALSE
  )

  fit <- GPvecchia_gp_fit(pm10 ~ coords.x1 + coords.x2, data = air_sf, m = 10)
  fit
}

}
