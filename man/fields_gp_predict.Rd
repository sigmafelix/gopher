% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fields_engine.R
\name{fields_gp_predict}
\alias{fields_gp_predict}
\title{Predict from a fields-fitted Gaussian Process model}
\usage{
fields_gp_predict(
  object,
  new_data,
  type = "numeric",
  level = 0.95,
  coord_cols = NULL,
  ...
)
}
\arguments{
\item{object}{A \code{"gopher_fields_fit"} object returned by \code{\link[=fields_gp_fit]{fields_gp_fit()}}.}

\item{new_data}{An \code{sf} object or \code{data.frame} with coordinates.}

\item{type}{\code{"numeric"} (default) or \code{"pred_int"}.}

\item{level}{Confidence level for prediction intervals (default \code{0.95}).}

\item{coord_cols}{Character(2) coord column names (non-sf path).}

\item{...}{Forwarded to \code{predict.Krig()} / \code{predictSE.Krig()}.}
}
\value{
A \code{\link[tibble:tibble]{tibble::tibble()}} with prediction columns.
}
\description{
Predict from a fields-fitted Gaussian Process model
}
\examples{
if (requireNamespace("spacetime", quietly = TRUE) &&
    requireNamespace("fields", quietly = TRUE)) {
  data("air", package = "spacetime")

  day_id <- which.max(colSums(!is.na(air)))
  air_day <- data.frame(
    station = rownames(air),
    pm10 = air[, day_id],
    day = dates[day_id],
    sp::coordinates(stations)
  )
  air_day <- air_day[stats::complete.cases(air_day$pm10), ]
  air_sf <- sf::st_as_sf(
    air_day,
    coords = c("coords.x1", "coords.x2"),
    crs = 4326,
    remove = FALSE
  )

  n_train <- floor(0.8 * nrow(air_sf))
  train_sf <- air_sf[seq_len(n_train), ]
  test_sf <- air_sf[seq.int(n_train + 1L, nrow(air_sf)), ]

  fit <- fields_gp_fit(pm10 ~ coords.x1 + coords.x2, data = train_sf)
  fields_gp_predict(fit, new_data = test_sf, type = "pred_int")
}

}
