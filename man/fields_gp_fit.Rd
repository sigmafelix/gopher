% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fields_engine.R
\name{fields_gp_fit}
\alias{fields_gp_fit}
\title{Fit a Gaussian Process model using the fields engine}
\usage{
fields_gp_fit(
  formula,
  data,
  covariance_function = "exponential",
  range = NULL,
  nugget = NULL,
  sill = NULL,
  coord_cols = NULL,
  use_mKrig = FALSE,
  ...
)
}
\arguments{
\item{formula}{A two-sided formula. Use \code{y ~ 1} for ordinary kriging;
covariates (e.g. \code{y ~ x1 + x2}) are passed to \code{fields::Krig()} as the
\code{Z} matrix (fixed effects / trend surface).}

\item{data}{An \code{sf} object or a \code{data.frame} with coordinate columns.}

\item{covariance_function}{Canonical covariance name. Defaults to
\code{"exponential"}. Note: \code{"spherical"} and \code{"gaussian"} are approximated
via \code{fields::Exp.cov}. You can also pass a covariance function directly
(e.g. \code{fields::Matern}), including through a one-sided formula used by
\code{parsnip} (e.g. \code{~fields::Matern}).}

\item{range}{Range (aRange) parameter for the covariance. \code{NULL} =
estimated by \code{fields::Krig()}.}

\item{nugget}{Nugget variance. Used to compute \code{lambda = nugget / sigma2}.}

\item{sill}{Partial sill (sigma2). \code{NULL} = estimated by \code{Krig}.}

\item{coord_cols}{Character(2) coordinate column names (non-sf path).}

\item{use_mKrig}{Logical. Use \code{fields::mKrig()} instead of \code{fields::Krig()}
for large datasets. Default \code{FALSE}.}

\item{...}{Additional arguments forwarded to \code{fields::Krig()} /
\code{fields::mKrig()}.}
}
\value{
A list of class \code{"gopher_fields_fit"}.
}
\description{
This function is called internally by parsnip. End users should call
\code{\link[parsnip:fit]{parsnip::fit.model_spec()}} on a \code{gaussian_process_spatial} specification
with \code{set_engine("fields")}.
}
\examples{
if (requireNamespace("spacetime", quietly = TRUE) &&
    requireNamespace("fields", quietly = TRUE)) {
  data("air", package = "spacetime")

  day_id <- which.max(colSums(!is.na(air)))
  air_day <- data.frame(
    station = rownames(air),
    pm10 = air[, day_id],
    day = dates[day_id],
    sp::coordinates(stations)
  )
  air_day <- air_day[stats::complete.cases(air_day$pm10), ]
  air_sf <- sf::st_as_sf(
    air_day,
    coords = c("coords.x1", "coords.x2"),
    crs = 4326,
    remove = FALSE
  )

  fit <- fields_gp_fit(pm10 ~ coords.x1 + coords.x2, data = air_sf)
  fit
}

}
