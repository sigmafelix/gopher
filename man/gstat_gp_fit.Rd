% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gstat_engine.R
\name{gstat_gp_fit}
\alias{gstat_gp_fit}
\title{Fit a Gaussian Process model using the gstat engine}
\usage{
gstat_gp_fit(
  formula,
  data,
  covariance_function = "exponential",
  range = NULL,
  nugget = NULL,
  sill = NULL,
  coord_cols = NULL,
  time_col = NULL,
  fit_variogram = TRUE,
  cutoff = NULL,
  width = NULL,
  ...
)
}
\arguments{
\item{formula}{A two-sided formula. Use \code{y ~ 1} for ordinary kriging and
\code{y ~ x1 + x2} for universal kriging.}

\item{data}{An \code{sf} object or a \code{data.frame} with coordinate columns.}

\item{covariance_function}{Canonical covariance name (see
\code{\link[=gaussian_process_spatial]{gaussian_process_spatial()}}). Defaults to \code{"exponential"}.}

\item{range}{Initial/fixed range parameter. \code{NULL} = auto-estimate.}

\item{nugget}{Initial/fixed nugget. \code{NULL} = auto-estimate.}

\item{sill}{Initial/fixed partial sill. \code{NULL} = auto-estimate.}

\item{coord_cols}{Character(2) column names for coordinates when \code{data} is
not \code{sf}. Auto-detected when \code{NULL}.}

\item{time_col}{Character name of the date/time column for spatiotemporal
kriging. \code{NULL} (default) = purely spatial.}

\item{fit_variogram}{Logical. When \code{TRUE} (default) the empirical
variogram is computed and the model is fitted even when \code{range},
\code{nugget}, and \code{sill} are all provided (so initial values are used as
starting values for optimisation). Set to \code{FALSE} to use the supplied
parameters without fitting.}

\item{cutoff}{Passed to \code{gstat::variogram()}: maximum distance for
the empirical variogram. \code{NULL} = gstat default (one-third of bounding
box diagonal).}

\item{width}{Passed to \code{gstat::variogram()}: lag width. \code{NULL} =
gstat default.}

\item{...}{Additional arguments forwarded to \code{gstat::fit.variogram()}.}
}
\value{
A list of class \code{"gopher_gstat_fit"} containing the fitted
variogram model, the original training data, and the formula.
}
\description{
This function is called internally by parsnip. End users should call
\code{\link[parsnip:fit]{parsnip::fit.model_spec()}} on a \code{gaussian_process_spatial} specification
with \code{set_engine("gstat")}.
}
\examples{
if (requireNamespace("spacetime", quietly = TRUE)) {
  data("air", package = "spacetime")

  # `air` is provided as legacy ST components (matrix + SpatialPoints + Date).
  # Convert one day to an `sf` object for spatial GP fitting.
  day_id <- which.max(colSums(!is.na(air)))
  air_day <- data.frame(
    station = rownames(air),
    pm10 = air[, day_id],
    day = dates[day_id],
    sp::coordinates(stations)
  )
  air_day <- air_day[stats::complete.cases(air_day$pm10), ]
  air_sf <- sf::st_as_sf(
    air_day,
    coords = c("coords.x1", "coords.x2"),
    crs = 4326,
    remove = FALSE
  )

  fit <- gstat_gp_fit(pm10 ~ 1, data = air_sf)
  fit
}

}
