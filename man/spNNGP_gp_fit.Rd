% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spNNGP_engine.R
\name{spNNGP_gp_fit}
\alias{spNNGP_gp_fit}
\title{Fit a Gaussian Process model using the spNNGP engine}
\usage{
spNNGP_gp_fit(
  formula,
  data,
  covariance_function = "exponential",
  range = NULL,
  nugget = NULL,
  sill = NULL,
  n_neighbors = 15L,
  n_samples = 1000L,
  n_burnin = 500L,
  method = "response",
  coord_cols = NULL,
  ...
)
}
\arguments{
\item{formula}{A two-sided formula. Covariates are included as fixed
effects (Universal Kriging).}

\item{data}{An \code{sf} object or \code{data.frame} with coordinate columns.}

\item{covariance_function}{Canonical covariance name. Defaults to
\code{"exponential"}.}

\item{range}{Starting value for the range parameter (\code{phi}).
\code{NULL} = uses a data-driven initial value.}

\item{nugget}{Starting value for the nugget (\code{tau.sq}).
\code{NULL} = 10 \\% of response variance.}

\item{sill}{Starting value for the partial sill (\code{sigma.sq}).
\code{NULL} = 90 \\% of response variance.}

\item{n_neighbors}{Number of nearest neighbours. Default \code{15}.}

\item{n_samples}{Number of MCMC samples. Default \code{1000}.}

\item{n_burnin}{MCMC burn-in. Default \code{500}.}

\item{method}{spNNGP method: \code{"response"} (default) or \code{"latent"}.}

\item{coord_cols}{Character(2) coordinate column names (non-sf path).}

\item{...}{Additional arguments forwarded to \code{spNNGP::spNNGP()}.}
}
\value{
A list of class \code{"gopher_spNNGP_fit"}.
}
\description{
This function is called internally by parsnip. End users should call
\code{\link[parsnip:fit]{parsnip::fit.model_spec()}} on a \code{gaussian_process_spatial} specification
with \code{set_engine("spNNGP")}.
}
\examples{
if (requireNamespace("spacetime", quietly = TRUE) &&
    requireNamespace("spNNGP", quietly = TRUE)) {
  data("air", package = "spacetime")

  day_id <- which.max(colSums(!is.na(air)))
  air_day <- data.frame(
    station = rownames(air),
    pm10 = air[, day_id],
    day = dates[day_id],
    sp::coordinates(stations)
  )
  air_day <- air_day[stats::complete.cases(air_day$pm10), ]
  air_sf <- sf::st_as_sf(
    air_day,
    coords = c("coords.x1", "coords.x2"),
    crs = 4326,
    remove = FALSE
  )

  fit <- spNNGP_gp_fit(
    pm10 ~ coords.x1 + coords.x2,
    data = air_sf,
    n_neighbors = 8,
    n_samples = 80,
    n_burnin = 40
  )
  fit
}

}
