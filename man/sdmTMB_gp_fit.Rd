% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/sdmTMB_engine.R
\name{sdmTMB_gp_fit}
\alias{sdmTMB_gp_fit}
\title{Fit a Gaussian Process model using the sdmTMB engine}
\usage{
sdmTMB_gp_fit(
  formula,
  data,
  covariance_function = "matern",
  range = NULL,
  nugget = NULL,
  sill = NULL,
  coord_cols = NULL,
  time_col = NULL,
  spatial = "on",
  spatiotemporal = "off",
  mesh_cutoff = NULL,
  n_knots = NULL,
  family = stats::gaussian(),
  share_range = FALSE,
  silent = TRUE,
  ...
)
}
\arguments{
\item{formula}{A two-sided formula. Use \code{y ~ 1} for a spatial-only model
without fixed effects (analogous to ordinary kriging), or include
covariates (e.g. \code{y ~ x1 + x2}) for a model with a fixed-effects trend
(analogous to universal kriging).}

\item{data}{An \code{sf} object or a \code{data.frame} with coordinate columns.}

\item{covariance_function}{Canonical covariance name (see
\code{\link{gaussian_process_spatial}}). sdmTMB uses a Matérn covariance via the
SPDE mesh; any value is accepted but mapped to \code{"matern"}. A warning is
issued for non-Matérn names.}

\item{range}{Unused by sdmTMB in the current adapter (estimated
internally via maximum likelihood). Kept for unified interface
compatibility.}

\item{nugget}{Unused by sdmTMB in the current adapter. Kept for unified
interface compatibility.}

\item{sill}{Unused by sdmTMB in the current adapter. Kept for unified
interface compatibility.}

\item{coord_cols}{Character vector of length 2 giving the coordinate column
names for non-\code{sf} input (x/longitude first, y/latitude second). \code{NULL}
triggers auto-detection.}

\item{time_col}{Character scalar naming the time column for spatiotemporal
models. \code{NULL} (default) = purely spatial.}

\item{spatial}{One of \code{"on"} (default) or \code{"off"}. Controls whether a
spatial random field is included.}

\item{spatiotemporal}{One of \code{"off"} (default), \code{"iid"}, \code{"ar1"}, or
\code{"rw"}. Controls the temporal structure of the spatiotemporal random
field. Ignored when \code{time_col} is \code{NULL}.}

\item{mesh_cutoff}{Numeric scalar passed to \code{sdmTMB::make_mesh()} as the
\code{cutoff} argument (minimum triangle edge length in coordinate units).
\code{NULL} = automatic (1/10 of the bounding box diagonal).}

\item{n_knots}{Integer. Number of mesh knots for the k-means mesh type.
If provided, \code{mesh_cutoff} is ignored and \code{type = "kmeans"} is used.}

\item{family}{A \code{family} object (default \code{stats::gaussian()}). Passed
directly to \code{sdmTMB::sdmTMB()}.}

\item{share_range}{Logical. Whether to share the spatial range between the
spatial and spatiotemporal random fields. Default \code{FALSE}.}

\item{silent}{Logical. Suppress model-fitting messages. Default \code{TRUE}.}

\item{...}{Additional arguments forwarded to \code{sdmTMB::sdmTMB()}.}
}
\value{
A list of class \code{"gopher_sdmTMB_fit"} containing:
\itemize{
\item \code{sdmtmb_fit} — the fitted \code{sdmTMB} model object.
\item \code{mesh}       — the \code{sdmTMBmesh} used for fitting.
\item \code{formula}    — the model formula.
\item \code{xy_cols}    — internal coordinate column names used in the data.
\item \code{coord_cols} — user-supplied \code{coord_cols} (may be \code{NULL}).
\item \code{time_col}   — the time column name (may be \code{NULL}).
\item \code{family}     — the \code{family} object.
}
}
\description{
This function is called internally by parsnip. End users should call
\code{\link[parsnip:fit.model_spec]{parsnip::fit.model_spec()}} on a \code{gaussian_process_spatial} specification
with \code{set_engine("sdmTMB")}.
}
\examples{
if (requireNamespace("sdmTMB", quietly = TRUE)) {
  train <- data.frame(
    x = runif(50, 0, 10),
    y = runif(50, 0, 10)
  )
  train$z <- sin(train$x) + cos(train$y) + rnorm(50, 0, 0.3)
  train_sf <- sf::st_as_sf(train, coords = c("x", "y"), crs = 4326)

  fit <- sdmTMB_gp_fit(z ~ 1, data = train_sf)
  fit
}
}
